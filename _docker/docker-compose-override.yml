# Copyright (c) 2025 Ekkodale GmbH. All rights reserved.
#
# This file is part of the gaeco platform system.
#
# Use of this file is governed by the terms of the license
# in LICENSE.md at the root of this repository.
# Unauthorized copying, modification, distribution, or use of this file,
# via any medium, is strictly prohibited except as expressly permitted
# under that license.

# docker compose -f docker-compose.yml -f docker‐compose-override.yml up -d
name: guideline-service
services:
  guideline-postgres:
    ports:
      - ${GUIDELINE_POSTGRES_OUTERPORT}:5432
  guideline-server:
    ports:
      - ${GUIDELINE_SERVER_OUTERPORT}:80
    environment:
      - Keycloak__ServerUrl=http://keycloak-server:8080
      - Keycloak__Authority=http://${KEYCLOAK_SERVER_HOSTNAME}/realms/${KEYCLOAK_REALM}
    build:
      context: ../Server
      dockerfile: Api/Dockerfile
      secrets:
        - nugetconfig

secrets:
  nugetconfig:
    file: ../../_docker/docker-compose-files/nuget.config
